{
  "name": "n8n-cloud-mcp",
  "version": "0.8.0",
  "description": "n8n MCP Server for Cloudflare Workers with 93 tools",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=neutral --format=esm --minify --outfile=dist/bundle.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.6",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  },
  "author": "Abound TechOlogy",
  "license": "MIT"
}
